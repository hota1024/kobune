{
  "name": "minato-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "mkdir -p public/logo && cp ../install.sh public/install.sh && cp ../assets/logo/*.svg public/logo/ && node scripts/og.mjs",
    "predev": "pnpm sync",
    "dev": "vitepress dev",
    "prebuild": "pnpm sync",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy:pages": "wrangler pages deploy .vitepress/dist --project-name=minato-docs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "vitepress": "^1.6.3",
    "vitepress-plugin-llms": "^1.13.5",
    "vue": "^3.5.41",
    "wawoff2": "^2.0.1",
    "wrangler": "^4.125.0"
  }
}
